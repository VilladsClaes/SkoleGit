@model IEnumerable<WebApplication1.Models.SizeTable>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SizeNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SizeNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SizeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SizeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SizeID })
        </td>
    </tr>
}

</table>
