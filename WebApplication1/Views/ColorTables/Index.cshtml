@model IEnumerable<WebApplication1.Models.ColorTable>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ColorHexCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ColorHexCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ColorID })
        </td>
    </tr>
}

</table>
